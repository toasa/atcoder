// 

#include <iostream>
#include <cmath>
#include <vector>
#include <algorithm>
#include <unordered_map>
#include <ios>
#include <iomanip>
#include <string>
#include <numeric>

using namespace std;

int N, M;
int S, T;

int main() {
    cin >> N >> M;
    int u[M];
    int v[M];

    for (int i = 0; i < M; i++) {
        cin >> u[i] >> v[i];
    }

    cin >> S >> T;

    return 0;
}
