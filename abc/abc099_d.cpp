// Good Grid

#include <iostream>
#include <cmath>
#include <vector>
#include <algorithm>
#include <unordered_map>
#include <ios>
#include <iomanip>
#include <string>
#include <numeric>

using namespace std;

int n, c;

int main() {
    cin >> n >> C;

    vector<vector<int>> d = vector<vector<int>>(c, vector<int>(c, 0));
    vector<vector<int>> c = vector<vector<int>>(n, vector<int>(n, 0));

    return 0;
}
