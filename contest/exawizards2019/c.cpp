// Snuke the Wizard

#include <iostream>
#include <cmath>
#include <vector>
#include <algorithm>
#include <unordered_map>
#include <ios>
#include <iomanip>
#include <string>
#include <numeric>

using namespace std;

int main() {
    int n, q;
    string s;

    cin >> n >> q;
    cin >> s;

    vector<char> t(q);
    vector<char> d(q);

    for (int i = 0; i < q; i++) {
        cin >> t[i] >> d[i];;
    }

    return 0;
}
